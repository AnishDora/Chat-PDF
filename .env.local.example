# Local environment example. Copy to `.env.local` and fill in values.

# Clerk (auth)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Supabase (DB + Storage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (RAG embeddings + answers)
# Keep this server-side only (no NEXT_PUBLIC_)
OPENAI_API_KEY=
